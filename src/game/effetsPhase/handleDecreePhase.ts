// import type { Dispatch, SetStateAction } from "react";
// import type { Card, Turn, Ranks } from "../../types/types";

/**
 * Gère la phase de décret : chaque joueur (IA ou humain) peut effectuer jusqu’à 2 actions.
 * La priorité est donnée au gagnant de l’initiative. L’IA agit automatiquement.
 *
 * @param turn Tour actuel ("player" ou "ai")
 * @param actionsPlayer Nombre d’actions restantes du joueur
 * @param actionsAi Nombre d’actions restantes de l’IA
 * @param initiativeWinner Qui a gagné l’initiative
 * @param setLogs Setter pour les messages dans le journal
 * @param setActionsPlayer Setter des actions du joueur
 * @param setActionsAi Setter des actions de l’IA
 * @param setTurn Setter du joueur actif
 * @param aiRanks Rangs de l’IA
 * @param playerRanks Rangs du joueur
 * @param setAiRanks Setter des rangs IA
 * @param setPlayerRanks Setter des rangs joueur
 * @param aiHand Main de l’IA
 * @param setAiHand Setter de la main IA
 */
export function handleDecreePhase() {}
// turn: Turn,
// actionsPlayer: number,
// actionsAi: number,
// initiativeWinner: Turn,
// setLogs: Dispatch<SetStateAction<string[]>>,
// setActionsPlayer: Dispatch<SetStateAction<number>>,
// setActionsAi: Dispatch<SetStateAction<number>>,
// setTurn: Dispatch<SetStateAction<Turn>>,
// aiRanks: Ranks,
// playerRanks: Ranks,
// setAiRanks: Dispatch<SetStateAction<Ranks>>,
// setPlayerRanks: Dispatch<SetStateAction<Ranks>>,
// aiHand: Card[],
// setAiHand: Dispatch<SetStateAction<Card[]>>,
// hasStarted,
// setHasStarted,
